# tests/e2e/

Testes end-to-end (Playwright).

Validam fluxos jogáveis do jogo no navegador e regressões visuais que tenham valor real de produto, garantindo que o adapter Phaser renderiza corretamente e que a integração core ↔ adapter funciona como esperado.
