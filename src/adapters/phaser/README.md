# src/adapters/phaser/

Renderização, input e scenes do Phaser 3.

Esta camada adapta o estado público do core para o jogador: desenha cartas, anima jogadas, captura toques/cliques e exibe a interface. Não contém regras de negócio — apenas reflete o que o core entrega.
